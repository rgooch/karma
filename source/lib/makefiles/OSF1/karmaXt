LIB = karmaXt

ifdef XINCLUDEPATH
CFLAGS = -I$(XINCLUDEPATH)
DEPENDFLAGS = -I$(XINCLUDEPATH)
endif
ifdef XLIBPATH
LDFLAGS = -L$(XLIBPATH) -lkarma -lXt -lXext -lX11
else
LDFLAGS = -lkarma -lXt -lXext -lX11
endif

include $(KARMAROOT)/source/lib/makefiles/$(OS)/GNUmakefile.rules
