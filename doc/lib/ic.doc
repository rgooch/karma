$PACKAGE          ic
$SUMMARY          Routines to generate icon pixmaps
$PURPOSE
    These routines are meant to provide a simple mechanism to generate standard
    icon bitmaps in X pixmap format.
$LIBRARY          karmaX11
$LINKWITH         -lkarmaX11
$END

$FUNCTION           ic_write_kplot_icon
$RETURN_TYPE        void
$PARAMETER_LIST
    (Display *display, Pixmap pixmap,
			  int width, int height)
$SUMMARY            Write the icon pixmap required for the <<kplot>> module.
$PURPOSE
    Write the icon pixmap required for the <<kplot>> module.
$PARAMETER          display :
    The X display.
$PARAMETER          pixmap :
    The pixmap to write to.
$PARAMETER          width :
    The width of the icon.
$PARAMETER          height :
    The height of the icon.
$RETURNS
    Nothing.
$END

$FUNCTION           ic_write_kscat_icon
$RETURN_TYPE        void
$PARAMETER_LIST
    (Display *display, Pixmap pixmap,
			  int width, int height)
$SUMMARY            Write the icon pixmap required for the <<kscat>> module.
$PURPOSE
    Write the icon pixmap required for the <<kscat>> module.
$PARAMETER          display :
    The X display.
$PARAMETER          pixmap :
    The pixmap to write to.
$PARAMETER          width :
    The width of the icon.
$PARAMETER          height :
    The height of the icon.
$RETURNS
    Nothing.
$END

$FUNCTION           ic_write_funcgen_icon
$RETURN_TYPE        void
$PARAMETER_LIST
    (Display *display, Pixmap pixmap,
			    int width, int height)
$SUMMARY            Write the icon pixmap required for the <<kfuncgen>> module.
$PURPOSE
    Write the icon pixmap required for the <<kfuncgen>> module.
$PARAMETER          display :
    The X display.
$PARAMETER          pixmap :
    The pixmap to write to.
$PARAMETER          width :
    The width of the icon.
$PARAMETER          height :
    The height of the icon.
$RETURNS
    Nothing.
$END

$FUNCTION           ic_write_kimage_icon
$RETURN_TYPE        void
$PARAMETER_LIST
    (Display *display, Pixmap pixmap,
			   int width, int height)
$SUMMARY            Write the icon pixmap required for the <<kview>> module.
$PURPOSE
    Write the icon pixmap required for the <<kview>> module.
$PARAMETER          display :
    The X display.
$PARAMETER          pixmap :
    The pixmap to write to.
$PARAMETER          width :
    The width of the icon.
$PARAMETER          height :
    The height of the icon.
$RETURNS
    Nothing.
$END

$FUNCTION           ic_write_karma_cm_icon
$RETURN_TYPE        void
$PARAMETER_LIST
    (Display *display, Pixmap pixmap,
			     int width, int height)
$SUMMARY            Write the icon pixmap required for the <<karma_cm>> module.
$PURPOSE
    Write the icon pixmap required for the <<karma_cm>> module.
$PARAMETER          display :
    The X display.
$PARAMETER          pixmap :
    The pixmap to write to.
$PARAMETER          width :
    The width of the icon.
$PARAMETER          height :
    The height of the icon.
$RETURNS
    Nothing.
$END

$FUNCTION           ic_write_datasource_icon
$RETURN_TYPE        void
$PARAMETER_LIST
    (Display *display, Pixmap pixmap,
			       int width, int height)
$SUMMARY            Write the icon pixmap required for generic data source modules.
$PURPOSE
    Write the icon pixmap required for generic data source modules.
$PARAMETER          display :
    The X display.
$PARAMETER          pixmap :
    The pixmap to write to.
$PARAMETER          width :
    The width of the icon.
$PARAMETER          height :
    The height of the icon.
$RETURNS
    Nothing.
$END

$FUNCTION           ic_write_datasink_icon
$RETURN_TYPE        void
$PARAMETER_LIST
    (Display *display, Pixmap pixmap,
			     int width, int height)
$SUMMARY            Write the icon pixmap required for generic data sink modules.
$PURPOSE
    Write the icon pixmap required for generic data sink modules.
$PARAMETER          display :
    The X display.
$PARAMETER          pixmap :
    The pixmap to write to.
$PARAMETER          width :
    The width of the icon.
$PARAMETER          height :
    The height of the icon.
$RETURNS
    Nothing.
$END

$FUNCTION           ic_write_datafilter_icon
$RETURN_TYPE        void
$PARAMETER_LIST
    (Display *display, Pixmap pixmap,
			       int width, int height)
$SUMMARY            Write the icon pixmap required for generic data filter modules.
$PURPOSE
    Write the icon pixmap required for generic data filter modules.
$PARAMETER          display :
    The X display.
$PARAMETER          pixmap :
    The pixmap to write to.
$PARAMETER          width :
    The width of the icon.
$PARAMETER          height :
    The height of the icon.
$RETURNS
    Nothing.
$END
