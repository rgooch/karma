$PACKAGE          d
$SUMMARY          Routines to control daemon mode
$PURPOSE
    These routines are meant to provide a mechanism for a process to enter
    daemon mode (ie. to dissociate from a controlling terminal).
$LIBRARY          karma
$LINKWITH         -lkarma
$END

$FUNCTION         d_enter_daemon_mode
$RETURN_TYPE      void
$PARAMETER_LIST
    ()
$SUMMARY          Enter daemon mode.
$PURPOSE
    This routine will cause the module to enter daemon mode. This
    involves detatching from a terminal.
$RETURNS
    Nothing.
$END

$FUNCTION         d_test_daemon_mode
$RETURN_TYPE      flag
$PARAMETER_LIST
    ()
$SUMMARY          Test if the module is running in daemon mode.
$PURPOSE
    Test if the module is running in daemon mode.
$RETURNS
    TRUE if the module is running in daemon mode, else FALSE.
$END
